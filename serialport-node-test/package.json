{
  "name": "serialport-node-test",
  "description": "test cross compilation of native modules for arm processors",
  "version": "0.1.1",
  "scripts": {
    "rebuild_arm": "cd ./node_modules/serialport/ && node-gyp clean configure --arch=arm rebuild",
    "package": "zip -r ./"
  },
  "dependencies": {
    "serialport": "3.1.2"
  }
}
